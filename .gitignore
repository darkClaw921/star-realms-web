node_modules/
.next/
dist/
out/
*.tsbuildinfo
.DS_Store
.env*.local

# Card artwork is (c) Wise Wizard Games. Downloaded locally for personal use only.
# The fetch script lives in the repo; the images must never be committed.
apps/web/public/cards/art/
# The art manifest describes those same files, so it is generated, never
# committed: scripts/art-manifest.mjs rebuilds it from whatever art is present
# and runs before dev, build and typecheck. A fresh clone with no art gets a
# valid empty manifest and compiles.
apps/web/src/cards/artManifest.gen.ts

# Persisted match logs
data/

# Verification output: the screenshots contain the same copyrighted card art.
reports/
.forge-worktrees/
